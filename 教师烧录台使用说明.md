# Desk Buddy 教师烧录台

孩子只需要使用公开网页，完成搭建、虚拟运行和代码创作。作品完成后，孩子点击顶部的“交给老师”，任选一种方式把代码给老师：复制后粘贴到消息里，或下载 `desk-buddy.ino` 文件。

老师只在自己的 Windows 电脑上烧录，因此不需要为每位孩子安装软件、注册账号或把主板授权给浏览器。

## 课前只需确认一次

这台老师电脑需要已经有：

1. Node.js LTS；
2. Arduino IDE，且已安装 ESP32 开发板支持和本课程示例所需的库；
3. 可传输数据的 USB 线，以及 ESP32-C3 Desk Buddy。

现有项目电脑已经具备本地编译服务。首次课程前，双击 `打开教师烧录台.cmd`，浏览器会打开 `http://127.0.0.1:4173/?teacher=1`。确认界面中出现“连接板子”和“烧录”两个按钮，再用一个示例完成一次测试烧录。

## 每次烧录

1. 双击 `打开教师烧录台.cmd`，保持它启动的最小化窗口运行。
2. 在教师烧录台点击“打开作品”，选择孩子发来的 `desk-buddy.ino`；如果孩子发来的是完整作品，也可以选择 `.json`。
3. 用 USB 数据线连接孩子的 Desk Buddy，点击“连接板子”，在浏览器弹窗选择 ESP32-C3。
4. 点击“烧录”，等待状态显示“烧录成功”。如果自动复位没有成功，按一下主板的 RST 按钮。

本机烧录台的编译器只运行在老师电脑上，代码不会上传到 GitHub Pages 或第三方编译服务。

## 对话固件（真机 SoftAP / I2S）

积木和代码示例仍用右边的「烧录」。对话模式请用中间栏的「连接板子」和「烧录对话固件」：

1. 切到「对话」，USB 接上 Desk Buddy；
2. 点「连接板子」，选择 ESP32-C3；
3. 点「烧录对话固件」，等成功后按 RST 或重新插 USB；
4. 屏幕出现 `DeskBuddy-XXXXXX`（芯片 MAC 后 6 位）和 `192.168.4.1` 后，用手机连热点并填家里的 Wi-Fi 与 MiniMax key；
5. 短按说话键听；长按约 4 秒忘记网络。

真机钥匙存在板子里，由板子自己访问 MiniMax。喇叭走功放 I2S，麦克风走 I2S，不走老师电脑的麦和喇叭。
