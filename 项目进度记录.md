# Desk Buddy 项目进度记录

更新日期：2026-09-12  
当前状态：已发布，可供孩子通过浏览器访问；本地教师烧录流程已保留。

## 对外链接

- 公开实验桌：https://naizhongyao.github.io/desk-buddy/
- 若浏览器仍缓存旧脚本，使用：https://naizhongyao.github.io/desk-buddy/?v=20260912-1
- GitHub 仓库：https://github.com/NaizhongYao/desk-buddy

GitHub Pages 已启用 HTTPS，并由 GitHub Actions 自动发布。向 main 分支推送新提交后，同一个公开链接会更新，无需购买域名。

## 已完成的孩子端功能

### 进入与介绍

- 首次进入会显示欢迎页，并播放竖屏介绍视频。
- 视频文件位于 dist/assets/intro.mp4，已随 GitHub Pages 一同发布，不依赖本地电脑。
- 孩子可以点击“去实验桌”，也可以选择下次不再自动显示；偏好保存在该浏览器本地。

### 虚拟实验桌

- 页面包含 OLED、ESP32-C3、功放、麦克风、喇叭五个零件。
- 零件以 3D 模型显示，支持俯视接线和 3D 观察。
- 面包板为 400 孔布局，包含中间端子区和四条电源轨；接线会检查孔位占用、金属条连通和元件摆放冲突。
- 可使用“自己接线”跟随 OLED 的 GND、VCC、SCL、SDA 指导，也可选“已经接好”加载参考答案。
- X-ray 透视可显示面包板内部连通关系。

### 知识库、代码与作品

- 顶部“零件知识库”包含机器人介绍、每个模块的用途、3D 视图、针脚说明、线材颜色说明和常见问题。
- 内置 OLED、LED、声音和麦克风相关示例。编辑器支持行号、Tab 缩进和 Ctrl/Cmd + Enter 运行。
- 可以保存完整作品为 .json，重新打开 .json 或 .ino。
- “交给老师”可复制当前代码，或下载 desk-buddy.ino。
- 虚拟运行支持本项目定义的 Arduino 教学语法子集，包括屏幕绘图、GPIO、串口、延时、条件与循环；它不是完整的 ESP32 仿真器。

## 教师烧录流程

孩子端的 GitHub Pages 不提供主板连接和烧录按钮，避免孩子误操作，也不需要云端编译器。

老师在 Windows 电脑双击 打开教师烧录台.cmd。该脚本启动本机编译服务，并打开：

http://127.0.0.1:4173/?teacher=1

只有这个本机教师地址会显示“连接板子”和“烧录”。老师打开孩子发来的 .ino 或 .json，连接 ESP32-C3 后完成编译与烧录。完整步骤见 教师烧录台使用说明.md。

本机烧录依赖 Node.js、Arduino IDE、ESP32 开发板支持和课程使用的 Arduino 库。已验证本机编译服务能为 ESP32-C3 输出 4 个烧录固件文件；本次记录未连接真实物理主板执行最后的写入动作。

## GitHub Pages 发布配置

- 工作流文件：.github/workflows/publish-pages.yml
- 发布内容：dist 目录
- 发布触发：向 main 推送，或手动运行工作流
- 已验证的应用资源提交：e6c72a2
- 本记录写入时最近成功工作流：34619568019

首次发布时，GitHub Pages 尚未启用，因此工作流首次失败。之后已用仓库账号启用 Pages，并重新执行工作流；目前 Pages 状态正常。

## 已修复的公开页面加载问题

### 问题现象

GitHub Pages 上曾出现零件模型、介绍视频、代码编辑器一直不加载；本地服务器有时正常。

### 根因

dist/vendor/GLTFLoader.js 原来引用了不存在的 ../utils/BufferGeometryUtils.js。浏览器请求该路径时得到 404。

由于 3D 加载器在 App 初始化阶段导入，模块加载失败会使后续初始化不继续执行，因此视频控制、代码初始内容和 3D 场景都会表现为没有加载。

### 修复内容

- 将 GLTFLoader 的真实依赖改为同目录的 ./BufferGeometryUtils.js。
- 新增 dist/utils/BufferGeometryUtils.js，作为旧缓存脚本的兼容入口。
- 给 App、知识库、3D 场景和 GLTFLoader 的模块地址加入版本查询参数，促使浏览器加载新模块。
- 入口 app.mjs 使用版本参数，避免旧版本入口脚本继续从缓存读取。

### 验证结果

通过 Playwright 分别检查 localhost 与公开 Pages，结果一致：

- 5 个 .glb 模型请求成功；
- 介绍视频开始播放；
- 示例代码已载入并可进入“运行中”；
- 知识库可以打开并点击 OLED 针脚；
- 页面 JavaScript 无运行错误；
- 无资源 404 或其他失败请求。

另外模拟了浏览器仍使用旧 GLTFLoader 的情形，兼容入口也能正常加载模型。

## 当前限制与后续注意事项

- GitHub Pages 是纯静态网站，不能在孩子设备上编译任意 ESP32 Arduino 程序或直接烧录；真实编译与烧录集中在教师电脑完成。
- 孩子端虚拟运行只支持教学语法子集。孩子写出的 .ino 在教师端首次真实编译时，仍可能出现真实 Arduino 库或硬件引脚相关错误。
- 若有人刚打开公开链接仍看到旧页面，请使用上方带版本号的链接，或在浏览器中执行强制刷新。

## 后续修改与发布方法

1. 修改 dist 下的页面、脚本或资源。
2. 在本地运行相关测试；加载功能可运行：node temp/check-loading.mjs http://127.0.0.1:4173/
3. 执行 git add、git commit、git push。
4. GitHub Actions 完成后，公开链接自动更新。

如果改动涉及脚本加载或静态资源路径，建议同时更新入口脚本的版本查询参数，避免课堂设备使用旧缓存。
