# 示例修复与音频案例说明

## 🐛 修复的问题

### 问题原因
虚拟运行时（runtime.mjs）**不支持全局变量初始化**。以下代码无法运行：
```cpp
int x = 64, y = 32;  // ❌ 全局变量
int dx = 2, dy = 2;  // ❌ 全局变量

void loop() {
  x += dx;  // 运行时找不到 x
}
```

### 修复方案
将全局变量移入 `setup()` 或 `loop()` 内作为局部变量：

#### ✅ 弹跳小球 - 修复后
```cpp
void loop() {
  int x = 64;  // ✅ 局部变量
  int y = 32;
  int dx = 2;
  int dy = 2;
  
  for (int i = 0; i < 200; i++) {  // 有限循环
    x = x + dx;
    y = y + dy;
    
    if (x <= 4 || x >= 124) dx = -dx;
    if (y <= 4 || y >= 60) dy = -dy;
    
    display.clearDisplay();
    display.fillCircle(x, y, 4, SSD1306_WHITE);
    display.drawRect(0, 0, 128, 64, SSD1306_WHITE);
    display.display();
    delay(30);
  }
}
```

#### ✅ 进度条 - 修复后
```cpp
for (int percent = 0; percent <= 100; percent = percent + 2) {
  // 使用 percent = percent + 2 替代 percent += 2
  // 避免潜在的运算符优先级问题
}
```

#### ✅ 心跳 - 修复后
```cpp
for (int y = 32; y <= 50; y++) {
  int w = 28 - (y - 32);  // 移除浮点数计算
  display.drawLine(54 - w/2, y, 54 + w/2, y, SSD1306_WHITE);
}
```

#### ✅ 计时器 - 修复后
```cpp
int seconds = millis() / 1000;  // 使用 int 替代 unsigned long
int mins = (seconds / 60) % 60;
int secs = seconds % 60;
```

### ❌ 移除的示例
**贪吃蛇** - 需要持久化的 x/y/dx/dy 状态，无法用局部变量实现连续移动

### ➕ 新增简单示例
**数字计数** - 0-99 大数字计数动画，展示 loop 循环

---

## 🔊 音频案例说明

### ⚠️ 重要提示
以下三个音频示例**只能烧录到真实板子上运行**，虚拟运行会报错：
- `driver/i2s.h` 是 ESP32 硬件库
- 虚拟运行时不支持 I2S、数组、结构体等高级语法
- 这些示例是**真实可用的完整代码**，可以直接烧录

### 📺 虚拟运行 vs 🔊 真实烧录

| 类型 | 虚拟运行（网页） | 真实烧录（板子） |
|------|-----------------|----------------|
| OLED 屏幕 | ✅ 支持 | ✅ 支持 |
| 板载 LED | ✅ 支持 | ✅ 支持 |
| 串口输出 | ✅ 支持 | ✅ 支持 |
| **喇叭/麦克风** | ❌ 不支持 | ✅ **支持** |
| **I2S 音频** | ❌ 不支持 | ✅ **支持** |

### 🔊 喇叭播放提示音
```cpp
// 接线：功放 LRC→7, BCLK→8, DIN→9, GND→G, Vin→3.3
// 功能：播放 440Hz A音，1秒间隔
// 用途：测试喇叭是否正常工作
```

### 🎤 麦克风显示音量
```cpp
// 接线：麦克风 WS→20, SCK→21, SD→10, GND→G, VDD→3.3
//       OLED SCL→4, SDA→5
// 功能：实时采集声音，计算音量，显示音量条
// 用途：可视化麦克风输入，制作声音可视化
```

### 🎤💡 声控灯（拍手亮灯）
```cpp
// 接线：麦克风 WS→20, SCK→21, SD→10
// 功能：检测拍手声，超过阈值时板载 LED 亮 2 秒
// 用途：声音触发，做互动项目（拍手开关灯）
```

---

## 📋 示例分组

### 📺 虚拟运行（9个）
可以在网页上点"运行"直接看效果：
- 会眨眼的伙伴
- 眼睛左右看
- 屏幕写字
- ⏰ 计时器
- 💗 跳动的心
- 📊 进度条
- 🎾 弹跳小球
- 🔢 数字计数
- 💡 板载灯闪烁

### 🔊 烧录到板子后可用（3个）
需要点"连接板子 → 烧录"，然后按 RST 按钮：
- 🔊 喇叭播放提示音
- 🎤 麦克风显示音量
- 🎤💡 声控灯

---

## 🎯 如何使用音频案例

1. **接线**：按注释接好功放/麦克风/OLED
2. **选择示例**：下拉框选择"🔊 喇叭播放提示音"或其他音频案例
3. **载入代码**：点"载入"
4. **烧录**：点"连接板子" → "烧录"
5. **复位**：烧录成功后按板子上的 RST 按钮
6. **测试**：
   - 喇叭案例：听到每秒一次的提示音
   - 麦克风案例：对着麦克风说话，看屏幕音量条变化
   - 声控灯：拍手，LED 亮 2 秒

---

## 📝 技术说明

### I2S 音频接线
```
功放 MAX98357：
  LRC  → GPIO 7   (Left/Right Clock)
  BCLK → GPIO 8   (Bit Clock)
  DIN  → GPIO 9   (Data In)
  GND  → G
  Vin  → 3.3V

麦克风 MSM3526：
  WS   → GPIO 20  (Word Select)
  SCK  → GPIO 21  (Serial Clock)
  SD   → GPIO 10  (Serial Data)
  GND  → G
  VDD  → 3.3V
```

### 为什么虚拟运行不支持音频？
- I2S 是硬件外设，需要 DMA、中断、定时器
- 音频采样率（16kHz）需要实时性
- 数组 `int16_t samples[320]` 超出虚拟运行范围
- 虚拟运行专注教学 OLED 绘图和基础逻辑

### 如果只想虚拟测试怎么办？
用 OLED 系列示例（前 9 个），烧录前可以在网页上完整测试逻辑和显示效果。
